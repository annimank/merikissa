import React from 'react';
import { Box, Typography } from '@mui/material';

function Art() {

    return (
        <Box>
            <Typography>Art</Typography>
        </Box>
    );
}

export default Art;
